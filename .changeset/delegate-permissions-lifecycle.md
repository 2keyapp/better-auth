---
"better-auth": minor
---

Add credential lifecycle endpoints to delegate-permissions plugin: revoke, renew, decommission, status check, and credential listing. Includes CSR-based machine enrollment with Platform CA cosigning, HAProxy trust anchor export, and full e2e test coverage.
