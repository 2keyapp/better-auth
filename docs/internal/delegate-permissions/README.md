# Unpublished product notes

These files are **not** part of the Better Auth docs site. They record fork-specific
tenant/billing notes. Public integration docs live at
`docs/content/docs/plugins/delegate-permissions.mdx`.

CLI product spec (`register` / `csr` / human vs machine vs mTLS): `2key-core-sdk/docs/CLI-PRODUCT.md`.

Step-by-step test commands (plugin Vitest, CLI enroll, openssl / mTLS, M2M frame, delegations): `2key-core-sdk/docs/TEST-USECASES.md`.
